# Active Inference Tests Workspace Configuration
# This workspace manages all test packages for the active inference framework
# Run all tests: gno test ./...
# Run specific module tests: gno test ./active_inference_core/
# Run with verbose output: gno test -v ./...
